package br.com.dio;

import br.com.dio.facade.Facade;
import br.com.dio.strategy.*;

public class Main {
    public static void main(String[] args) {
    /*
    * Padrão Singleton garante que uma classe tenha apenas uma única instância durante
    * toda a execução do programa e fornece um ponto central de acesso a ela.


    SingletonLazy lazy = SingletonLazy.getInstancia();
        System.out.println(lazy);
        lazy = SingletonLazy.getInstancia();
        System.out.println(lazy);

        SingletonEager eager = SingletonEager.getInstancia();
        System.out.println(eager);
        eager = SingletonEager.getInstancia();
        System.out.println(eager);

        SingletonLazyHolder holder = SingletonLazyHolder.getInstancia();
        System.out.println(holder);
        holder = SingletonLazyHolder.getInstancia();
        System.out.println(holder);

    * Padrão Strategy permite mudar o comportamento de um objeto durante
    * a execução do programa sem precisar alterar a estrutura da classe principal.

        Comportamento normal = new ComportamentoNormal();
        Comportamento defensivo = new ComportamentoDefensivo();
        Comportamento agressivo = new ComportamentoAgressivo();

        Robo robo = new Robo();
        robo.setComportamento(normal);
        robo.mover();
        robo.mover();

        robo.setComportamento(defensivo);
        robo.mover();

        robo.setComportamento(agressivo);
        robo.mover();
        robo.mover();
        robo.mover();


    * Padrão Facade fornece uma interface simplificada e unificada para um conjunto
    * complexo de subsistemas (como APIs externas, serviços de CRM, consultas de CEP e banco de dados).

        Facade facade = new Facade();
        facade.migrarCliente("Pessoa","12345567");
       */
    }
}